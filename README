# Fichier des specs
pyi-makespec --onefile -windowed --name Assistant main.py

# client lourd execution
pyinstaller `
--onefile `
--windowed `
--name "Assistant" `
--icon "assets/icon.ico" `
--add-data "frontend;frontend" `
--add-data "data;data" `
main.py